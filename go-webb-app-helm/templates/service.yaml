apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-webb-app-helm.fullname" . }}
  labels:
    {{- include "go-webb-app-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-webb-app-helm.selectorLabels" . | nindent 4 }}
