apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-webb-app-helm.fullname" . }}-test-connection"
  labels:
    {{- include "go-webb-app-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-webb-app-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
